name = "cf-v2ray"
main = "Cloudflare_vless__worker.js"
compatibility_date = "2024-06-04"

# Set uuid and proxyip in Cloudflare Dashboard → Worker → Settings → Variables,
# or via: wrangler secret put uuid
